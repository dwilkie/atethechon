#!/usr/bin/env ruby

# Usage:

# $ scrape_data

require "bundler/setup"
require "atethechon"
require "yaml"

File.write(
  "data/world_bank.yml",
  Atethechon::ContactsSerializer.new(
    contacts: Atethechon::WorldBankScraper.new.scrape,
    organization_identifier: :world_bank
  ).to_yaml
)
